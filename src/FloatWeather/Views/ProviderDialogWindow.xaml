<Window x:Class="FloatWeather.Views.ProviderDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="数据源配置" Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        ShowInTaskbar="False"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextLowBrush}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12,10,14,14">
        <!-- 柔和投影：多层半透明圆角，替代 DropShadowEffect（避免透明窗口的灰色直角） -->
        <Border CornerRadius="14" Margin="2,4,0,0" Background="{StaticResource ShadowSoftBrush}"/>
        <Border CornerRadius="14" Margin="1,2,0,0" Background="{StaticResource ShadowHardBrush}"/>
        <Border CornerRadius="14" Background="{StaticResource AppBgSoftBrush}"
                BorderBrush="{StaticResource StrokeStrongBrush}" BorderThickness="1"
                Padding="18,14">
        <StackPanel>
            <!-- 标题 -->
            <TextBlock Text="{Binding DialogTitle}" FontSize="14" FontWeight="SemiBold"
                       Foreground="{StaticResource TextHighBrush}" Margin="0,0,0,14"/>

            <!-- 字段列表 -->
            <ItemsControl ItemsSource="{Binding Fields}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="{Binding Label}" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="{Binding Multiline}">
                                <TextBox.Style>
                                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                        <Setter Property="MinHeight" Value="32"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Multiline}" Value="True">
                                                <Setter Property="TextWrapping" Value="Wrap"/>
                                                <Setter Property="MaxHeight" Value="88"/>
                                                <Setter Property="VerticalContentAlignment" Value="Top"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- 操作按钮 -->
            <Grid Margin="0,6,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Style="{StaticResource GhostButton}"
                            Margin="0,0,10,0" IsCancel="True" Click="Cancel_Click"/>
                    <Button Content="确定" Style="{StaticResource PrimaryButton}"
                            IsDefault="True" Click="Ok_Click"/>
                </StackPanel>
            </Grid>
        </StackPanel>
        </Border>
    </Grid>
</Window>
