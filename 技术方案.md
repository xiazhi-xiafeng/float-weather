# float-weather 悬浮天气 · 技术方案

> Windows 桌面悬浮天气小组件，聚合多天气数据源，提供主备切换与故障自动降级能力。

---

## 1. 项目概述

### 1.1 目标
打造一个 Windows 桌面端**可拖拽悬浮天气小组件**：常驻桌面显示实时天气，点击展开小时/多日预报、空气质量、生活指数等详情；多天气源协同，保证数据实时、稳定、可用。

### 1.2 核心定位
- **悬浮小组件（主）**：置顶、半透明、可拖拽、鼠标穿透开关，单行所见即所得。
- **详情主窗口（次）**：完整天气信息，楼层式布局。

### 1.3 数据源（共 7 个）
| 来源 | 类型 | 鉴权 | 覆盖 |
|------|------|:---:|------|
| 和风天气 QWeather | 官方开放接口 | JWT/Ed25519 | 实时+24h逐时+7日+空气质量+生活指数 |
| 高德天气 Amap | 官方开放接口 | Web服务 Key | 全国城市实时+3日 |
| 心知天气 Seniverse | 官方开放接口 | API Key | 实时+逐日+逐时（免费版限市级） |
| OpenWeather | 官方开放接口 | API Key | 全球实时+预报 |
| Open-Meteo | 免费开放接口 | 无 | 全球实时+预报（坐标） |
| wttr.in | 免费开放接口 | 无 | 全球实时+逐时+逐日（中文地名直查） |
| 中国天气网 ChinaWeather | 免费开放接口 | 无 | 实时+逐时+逐日 |

> 默认优先级：和风 > 高德 > 心知 > OpenWeather > Open-Meteo > wttr.in > 中国天气网。其中和风字段最全、免费额度充足，作为默认主数据源；Open-Meteo 与 wttr.in 免 Key 兜底保证任何 Key 未配置也能取数；中国天气网接口脆弱、明文 http，压至**末位兜底**。

---

## 2. 技术选型（环境已确认）

### 2.1 选型结论：**C# / WPF (.NET 8)**

本机环境：`.NET SDK 8.0.301`、`Rust 1.95.0`、`Node 24 / npm 11` 均可用；C#/WPF 与 Tauri 都具备运行条件。最终选择 **WPF**，理由：

| 需求 | WPF 支持度 | Tauri 支持度 |
|------|-----------|--------------|
| 无边框 + 透明 + 不规则窗体 | ⭐ 原生 `AllowsTransparency + WSB` | 需原生/Rust 插件桥接 |
| 始终置顶 `Topmost` | ⭐ 一行属性 | 需桥接 |
| 鼠标穿透 `WS_EX_TRANSPARENT` | ⭐ P/Invoke 简单 | 需桥接 |
| 系统托盘常驻 `NotifyIcon` | ⭐ 原生 | 需插件 |
| 开机自启（注册表/启动项） | ⭐ 原生 `Registry` | 需插件 |
| 常驻内存/稳定性 | ⭐ 轻量、托管 | WebView 偏高 |
| 矢量天气图标动态动画 | ⭐ XAML + 双缓冲 | 前端更强 |

### 2.2 技术清单
- **语言/框架**：C# 12 + .NET 8 + WPF
- **UI 渲染**：XAML，`CommunityToolkit.Mvvm` 绑定（`net8.0-windows`）
- **数据访问**：`HttpClient` + `IHttpClientFactory` + `System.Text.Json`（源适配器模式）
- **DI/配置**：`Microsoft.Extensions.DependencyInjection` + `Microsoft.Extensions.Configuration.Json`（收敛到 8.0.x）
- **日志**：`Serilog`（滚动文件，保留 7 天）
- **图标**：和风 `qweather-icons.ttf` 图标字体 + emoji 兜底（避免位图模糊）
- **打包**：绿色免安装运行（待 Inno Setup / 分发评估）

---

## 3. 天气数据源接入方案

### 3.1 各源接口要点

#### ① 和风天气 QWeather（默认主源，JWT 认证）
- 实时：`GET {ApiHost}/v7/weather/now?location={id}`
- 24h 逐时：`/v7/weather/24h`；7日：`/v7/weather/7d`；空气质量：`/v7/air/now`；生活指数：`/v7/indices/1d?type=0`
- 鉴权：`Authorization: Bearer <JWT>`，JWT 由 `ProjectId/CredentialId/PrivateKey`（Ed25519）构建，`ApiHost` 为开发者专属域名
- 城市：`geo/v2/city/lookup` 查 `LocationID`（新城市编码）；区/县级地址先按末级区县优先解析
- **部分降级**：实时/逐时/逐日为核心接口必须成功；空气质量/生活指数为辅助接口，失败仅丢弃该项，不弃用整源

#### ② 高德天气 Amap
- 实时：`GET https://restapi.amap.com/v3/weather/weatherInfo?key={key}&city={adcode}&extensions=base`
- 预报：`extensions=all`（未来 3 天）
- 需：高德开放平台「Web服务」Key；城市用 `adcode`（行政区划码），由地理编码接口解析；解析失败可直接用城市名兜底

#### ③ 心知天气 Seniverse
- 实时/逐日/逐时：`GET https://api.seniverse.com/v3/weather/{now|daily|hourly}.json?key={key}&location={loc}`
- 需：`Key`；`location` 直接接受中文城市名
- 限制：免费版**仅市级**数据，区/县级返回 403/AP010002 → 自动**逐级降级短名**解析；不对 hourly/daily 权限时优雅降级为空，不掉主流程

#### ④ OpenWeather
- 地理编码：`http://api.openweathermap.org/geo/1.0/direct?q={city}&limit=5&appid={key}`（不支持裸中文 q）
- 实时：`data/2.5/weather?lat=&lon=&units=metric&lang=zh_cn`
- 需：`appid`；中文城市名的歧义（如"长洲"港/桂同名）用**启发式打分**挑选（中文名命中 > 区县级尾缀 > 中国大陆），并补查 "{name}县" 合并打分

#### ⑤ Open-Meteo（免费，无 Key）
- 地理编码：`https://geocoding-api.open-meteo.com/v1/search?name={q}&count=10&language=zh`
- 实时/预报：`https://api.open-meteo.com/v1/forecast?latitude=&longitude=&current=&hourly=&daily=`
- 同名歧义（如"长沙"）用**行政区/特征码/人口打分**挑选主城；命中不到区县级时回退 wttr.in 与高德坐标

#### ⑥ wttr.in（免费，无 Key）
- 地名直查：`https://wttr.in/{city}?format=j1&m`，自带中文地名库，覆盖区县，免本地坐标解析
- `nearest_area[].latitude/longitude` 亦可复用为其他源的坐标兜底
- 天气文本为英文，统一映射为和风 icon code 并渲染中文

> ⑦ 中国天气网 ChinaWeather（免费，无 Key）
> - 实时/逐时/逐日：`http://toy1.weather.com.cn/...`（非官方、明文 http、响应不稳定）
> - 已实现但**仅作末位兜底**：接口脆弱且无官方保障，默认排在 Open-Meteo / wttr.in 之后，任一稳定的免 Key 源不可用再轮到它。

### 3.2 多源协同策略：主备 + 故障自动降级（已实现）

```
【调度核心】SourceManager
   ├── 优先级（可配置主/备）: 和风 > 高德 > 心知 > OpenWeather > Open-Meteo > wttr.in > 中国天气网
   ├── 单请求超时: 5s（含城市解析+取数）
   ├── 失败计数 + 熔断: 连续 3 次失败 → 熔断 60s（冷却后自动恢复）
   └── 健康状态追踪 per-source（可读快照）
```

执行流程：
1. 按优先级取**当前健康源**发起请求（默认和风）。
2. 超时或失败 → 记一次失败并**自动降级到下一源**重试。
3. 连续失败达阈值 → 该源进入**熔断**（60s），避免反复请求；冷却结束自动恢复。
4. 结果做可用性校验，异常直接弃用转下一源。
5. `WeatherService` 保留最近一次成功结果做内存缓存，断源时展示「上次数据」而非空白。

> 结构用「**适配器模式**」：`IWeatherProvider` 统一接口，各源独立实现 `GetWeatherAsync`，新增数据源只加一个类。城市解析由 `CityResolver` 提供强类型方法，各源直接调用，不再依赖源名字符串分发。

---

## 4. 系统架构（分层）

```
┌───────────────────────────────────────────────┐
│  UI 层 (WPF)                                  │
│   FloaterWindow(悬浮)  MainWindow(详情)       │
│   SettingsWindow(设置)  ProviderDialog(源弹窗) │
│   ViewModels(MVVM)  Converters                │
├───────────────────────────────────────────────┤
│  应用服务层                                     │
│   WeatherService   SourceManager   ConfigService│
│   CityResolver     ThemeResolver    UiState     │
│   IconService       AutoStartService            │
├───────────────────────────────────────────────┤
│  数据接入层 (适配器)                            │
│   IWeatherProvider                            │
│   ├ QWeatherProvider  ├ AmapProvider           │
│   ├ SeniverseProvider ├ OpenWeatherProvider    │
│   ├ OpenMeteoProvider ├ WttrInProvider         │
│   └ ChinaWeatherProvider（末位兜底）            │
│   HttpClient + System.Text.Json                │
├───────────────────────────────────────────────┤
│ 基础设施层                                      │
│   DI / 配置 / Serilog / 缓存 / 系统集成         │
│   (托盘、开机自启、单实例 Mutex、透明置顶)       │
└───────────────────────────────────────────────┘
```

**依赖原则**：高层只依赖 `IWeatherProvider` 接口与 DTO，不感知具体源 → 便于主备切换与扩展。

---

## 5. 功能模块

| 模块 | 职责 | 状态 |
|------|------|:---:|
| **悬浮小组件** | 实时温度/天气图标，可拖拽、置顶、半透明、鼠标穿透 | ✅ |
| **详情窗口** | 逐时、多日、空气质量、风力、湿度、生活指数 | ✅ |
| **多源管理与降级** | 主备切换、熔断、自动降级、内存缓存 | ✅ |
| **城市管理** | 手动输入城市名，各源自动解析其城市 ID/坐标（区县优先、同名打分） | ✅ |
| **设置中心** | 数据源顺序与 Key、刷新间隔、城市、开机自启、鼠标穿透 | ✅ |
| **系统托盘** | 常驻、菜单、退出 | ✅ |
| **自动刷新** | DispatcherTimer 定时刷新（默认 30 分钟，防重入） | ✅ |
| **开机自启** | 注册表 HKCU\Run 开关 | ✅ |
| **单实例** | 命名 Mutex，重复启动自动退出 | ✅ |
| **磁盘缓存/离线** | 断网长期展示 | ⏳ 仅内存缓存 |

---

## 6. 目录结构

```
float-weather/
├─ 技术方案.md
├─ src/
│  └─ FloatWeather/                  # 主程序 (WPF, net8.0-windows)
│     ├─ App.xaml(.cs)               # 启动、DI、单实例 Mutex、全局异常兜底
│     ├─ MainWindow.xaml(.cs)        # 详情主窗口
│     ├─ Converters/
│     │  └─ WeekdayConverter.cs      # 中文星期转换
│     ├─ Models/Dto/
│     │  ├─ WeatherModels.cs         # 统一 WeatherNow/Forecast/Index
│     │  └─ Sources/                 # 各源私有响应模型
│     ├─ Providers/                  # IWeatherProvider + 7 源实现 + Json
│     ├─ Services/
│     │  ├─ WeatherService.cs        # 业务封装 + 缓存 + 请求版本保护
│     │  ├─ SourceManager.cs         # 主备/降级/熔断核心
│     │  ├─ CityResolver.cs          # 各源城市/坐标解析 + 缓存 + 打分
│     │  ├─ ConfigService.cs         # 配置读写（appsettings + user-config）
│     │  ├─ IconService.cs / IconResolver.cs  # 图标字体与 emoji 映射
│     │  ├─ QWeatherJwt.cs           # 和风 JWT 签发
│     │  ├─ UiStateService.cs
│     │  └─ AutoStartService.cs
│     ├─ Theme/                      # 动态天气配色
│     ├─ ViewModels/                 # Floater/Detail/Settings + Provider 字段
│     ├─ Views/                      # Floater/Settings/ProviderDialog 窗口
│     ├─ Resources/                  # 图标资源
│     └─ appsettings.json            # Key、源顺序、刷新间隔
└─ tools/
   └─ ProviderTester/                # 多源 × 多地名取数回归测试（写 results.txt）
```

---

## 7. 数据模型设计（统一模型）

```csharp
public sealed record WeatherNow
{
    string City; string LocationId;
    decimal Temp;            // °C
    decimal FeelsLike;
    string WeatherText;      // 晴/多云...
    string IconCode;         // 映射到图标字体
    string WindDir; decimal WindScale;  // 风力
    int Humidity;            // %
    int Aqi; string AqiCategory; string AqiPrimary;
    DateTime ObservedTime;  // 数据时间
    string Source;          // 数据来源标识(调试/标记)
}

public sealed record ForecastHour
{
    DateTime Time; string IconCode; string Text; decimal Temp;
}

public sealed record ForecastDay
{
    DateTime Date; string IconCode; string Text;
    decimal TempMin, TempMax;
    string WindDir; decimal WindScale;
}

public sealed record WeatherIndex
{
    string Name; string Level; string Text;
}

// 聚合载荷
public sealed record WeatherResult
{
    WeatherNow? Now; List<ForecastHour> Hourly;
    List<ForecastDay> Daily; List<WeatherIndex> Indices;
    string Source; DateTime FetchedAt;
}
```

---

## 8. 界面设计

### 8.1 悬浮小组件 FloaterWindow
- 无边框 + `AllowsTransparency=true`，`WindowStyle=None`，背景半透明，玻璃拟态配色。
- 默认右下角，支持拖拽移动（记忆上次位置）。
- 单行布局：`天气图标 | 温度 | 天气描述 | 城市`；悬停/点击展开逐时浮层与详情。
- **鼠标穿透**开关：开启后鼠标事件穿透到底层，仅托盘可交互。
- 自动刷新：DispatcherTimer 按 `RefreshInterval` 刷新，`IsBusy` 防重入。
- **边缘吸附**：拖拽结束距屏幕边缘 ≤ 阈值（`UiState.SnapThreshold`，默认 12px，0=关闭，设置页可调 0–60）时自动对齐到该边并持久化。
- **亮度采样事件化**：桌面直显下改为"拖拽结束/显形"事件驱动 + 300ms 防抖采样，四周角取色避开窗自身像素，替代常驻 2s 轮询（省 CPU）。
- **显示器兜底**：订阅 `SystemEvents.DisplaySettingsChanged`，热插拔/分辨率变更后窗口掉出虚拟屏即复位到主屏右下角并持久化。

> 真机验证（吸附滑块边界，DPI 144/150%）：把悬浮窗拖到距屏幕左边缘 20 物理 px 处——
> - `SnapThreshold=0` → 停在 20px（不吸附，`threshold<=0` 直接返回）。
> - `SnapThreshold=60` → 20px < 60 阈值，松手吸附贴齐左边缘（编程 Left=VirtualScreenLeft=0，物理 0px）并持久化。
> 验证方法：启动前将 `SnapThreshold` 写入 `ui-state.json`（与滑块同一状态源）→ 用 user32 `SetCursorPos`+`mouse_event` 模拟拖拽 → `GetWindowRect` 读取最终落点判断 `dist_from_left`≈0（吸附）或≈落点（未吸附）。

- **右键菜单**：`Border`（卡片）上挂 WPF `ContextMenu`，与托盘菜单同构——显示/隐藏悬浮窗、打开详情、设置、鼠标穿透、桌面直显、开机自启、刷新天气、退出；打开时经 `SyncContextMenu()` 同步各开关当前状态（显示/隐藏文案切换、穿透/直显/自启勾选）。
- **双击打开详情**：`Border` 无 `MouseDoubleClick` CLR 事件，故在 `DragMove_MouseLeftButtonDown` 内判 `e.ClickCount >= 2` 直接 `OpenDetail()`，单击才进入 `DragMove` 拖拽，二者互不冲突；`OpenDetail` 带防重（`_detailWindow` 已加载则 `Activate` 激活而非重复建窗）。
- **低透明度 hover 显形**：`Opacity < max(_vm.Opacity, 0.9)` 时鼠标移入临时提亮到该阈值、移出置回 `_vm.Opacity`（穿透开启时不触发）。
- **逐时浮层延迟关闭**：`MouseLeave` 起 250ms `_hourlyCloseDebounce` 再关 `HourlyPopup`，鼠标从卡片移到浮层途中不闪关。
- **穿透泄漏提示**：穿透开启时启动 600ms `_ctHoverTimer`，鼠标落在本窗物理矩形内（`GetWindowRect`+`GetCursorPos`）给一次托盘气泡"解除请点托盘"，移出复位可再次提示。
- **温度转场动画**：温度 `TextBlock` 绑定加 `NotifyOnTargetUpdated=True`，`TargetUpdated` 时对 `TempText` 做 300ms 0.4→1 的 `CubicEase` 淡入。
- **裸屏逐时浮层描边**：`IsBare` 时浮层主 Border 保留描边 `#26000000`（厚度 1）以匹配深色文字，不再完全透明。

> 真机验证（右键菜单 + 双击，user32 `SetCursorPos`+`mouse_event` 模拟 + `EnumWindows` 检测新增窗口）：
> - 右键于卡片右上（rcX,midY）→ `EnumWindows` 出现新窗 `w=159 h=204`，落点 (1826,59) 正对点击处（WPF ContextMenu 为 `HwndWrapper` 类窗口，非原生 `#32768`）。
> - 双击于卡片左中部 → 新增详情窗 `w=408 h=620`（`GetWindowRect` 确认），防重守卫随窗 `Closed` 清空引用。

### 8.2 详情窗口 MainWindow
- 顶部：当前实时卡片；中部「逐时」横向滚动；下部「多日」+「生活指数」。
- 顶栏标注当前来源、数据时间与更新状态。

### 8.3 主题
- 亮/暗（动态天气配色）：随天气类型与昼夜自动切换背景氛围色。

---

## 9. 关键技术点

| 技术点 | 实现方式 |
|--------|----------|
| 无边框透明窗体 | `WindowStyle=None; AllowsTransparency=true; Background=Transparent` |
| 圆角投影 | 内部 `Border CornerRadius` + 半透明边框叠层 |
| 始终置顶 | `Topmost=true` |
| 鼠标穿透 | P/Invoke 设 `WS_EX_TRANSPARENT | WS_EX_LAYERED` |
| 拖拽移动 | `MouseMove` 记录偏移更新坐标，位置持久化 |
| 单实例 | 命名 `Mutex`（`Local` 前缀按会话隔离），已存在则退出 |
| 开机自启 | 注册表 `HKCU\Run` 写当前 exe 路径 |
| 系统托盘 | `NotifyIcon`，菜单：打开/刷新/设置/退出 |
| 定时刷新 | `DispatcherTimer`，`IsBusy` 锁定避免重入 |
| 多源降级/熔断 | `SourceManager`：优先级、5s 超时、失败计数、60s 熔断冷却 |
| 部分降级 | 和风辅助接口失败仅丢弃该项，核心照常返回 |
| 帧竞态保护 | `WeatherService` 请求版本号，避免过期结果覆盖新快照 |
| 自定义滚动条 | 玻璃拟态滚动条，悬停显示 |

---

## 10. 开发进度（里程碑）

| 阶段 | 内容 | 状态 |
|------|------|:---:|
| M1 骨架 | 解决方案、DI、配置、单一源（和风）+ 基础悬浮窗 | ✅ |
| M2 多源接入 | 高德 / 心知 / OpenWeather / Open-Meteo / wttr.in / 中国天气网 适配器 + 统一模型 | ✅ |
| M3 降级核心 | SourceManager：优先级、超时、失败计数、熔断、缓存 | ✅ |
| M4 交互完善 | 拖拽、置顶、托盘、设置页、位置持久化、详情窗口 | ✅ |
| M5 打磨 | 城市解析（区县优先/同名打分）、主题、图标字体、日志、单实例、部分降级 | ✅ |

> 中国天气网已实现并作**末位兜底**源；磁盘级离线缓存为后续增强项。

---

## 11. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 免费 Key 配额用尽 | 数据取不到 | 多源自动降级兜底，提示用户续费/配置新 Key |
| 悬浮窗交互与穿透冲突 | 操作体验 | 穿透做成显式开关，默认交互正常 |
| API 返回结构变化 | 解析失败 | 统一 DTO 隔离 + Serilog 记录异常报文便于排查 |
| 城市名同名歧义 | 跨源城市匹配错 | `CityResolver` 打分挑选（区县尾缀/行政区/特征码/人口） |
| 辅助接口不稳定 | 影响整源 | 和风辅助接口部分降级，仅丢弃该项数据 |

---

## 12. 备注 / 待确认
- 各源 Key 由用户本人在对应开放平台申请，写入 `appsettings.json`（本地配置，不入库）；用户修改写入可写的 `user-config.json`。
- 刷新间隔、数据源顺序、城市、主题等均可在设置中调整。
- 分发方式：当前为绿色免安装直跑；MSIX / Inno Setup 分发待评估。